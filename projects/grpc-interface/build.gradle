buildscript {
    ext {
        protobufPluginVersion = '0.9.3'
    }
}

plugins {
    id 'com.google.protobuf' version "${protobufPluginVersion}"
    id 'org.jetbrains.kotlin.jvm' version '1.8.21'
}

group 'com.pollvite'
version = '0.0.1-SNAPSHOT'

dependencies {
    implementation mavenDeps.grpcProtobuff
    implementation mavenDeps.grpcStub
    compileOnly mavenDeps.javaxAnnotationApi // Workaround since spring boot 3 does not use javax
    implementation "org.jetbrains.kotlin:kotlin-stdlib"
}

protobuf {
    protoc {
        artifact = mavenDeps.protobufProtoc
    }
    generatedFilesBaseDir = "$projectDir/src/generated"
    clean {
        delete generatedFilesBaseDir
    }
    plugins {
        grpc_java {
            artifact = mavenDeps.protocGenGrpcJava
        }
    }
    generateProtoTasks {

        all().each { task ->
            task.plugins {
                grpc_java {}
            }
            task.builtins {
                java {}
            }
        }
    }
}

idea {
    module {
        sourceDirs += file("src/generated/main/java")
        sourceDirs += file("src/generated/main/grpc_java")
        generatedSourceDirs += file("src/generated/main/java")
        generatedSourceDirs += file("src/generated/main/grpc_java")
    }
}