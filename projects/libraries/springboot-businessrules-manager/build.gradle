group = 'com.pollvite'
version = '0.0.1-SNAPSHOT'

jar {
    archivesBaseName = 'shared-models-mongo'
}

dependencies {
    annotationProcessor mavenDeps.springBootAutoConfigureProcessor
    implementation mavenDeps.springBootStarter
    implementation project(':shared-errors')
}